(when (not (package-installed-p 'simple-httpd)) (package-install 'simple-httpd))
(when (not (package-installed-p 'js2-mode)) (package-install 'js2-mode))
(when (not (package-installed-p 'skewer-mode)) (package-install 'skewer-mode))
